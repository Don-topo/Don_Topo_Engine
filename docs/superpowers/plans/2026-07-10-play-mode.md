# Play Mode Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Botón toolbar Play/Stop que activa/desactiva física+audio, restaurando la escena al estado previo a Play cuando se pulsa Stop (tipo Unity Play-In-Editor).

**Architecture:** `Scene` gana `toJson()`/`fromJson(...)` públicos (extracción sin cambio de comportamiento de la lógica ya en `save`/`load`) — el snapshot de Play Mode vive en RAM (`nlohmann::json`), no en fichero. `EditorUI` gana el estado `m_isPlaying`/`m_playSnapshot` y un botón toolbar que en Play captura `m_scene->toJson()` y en Stop llama un método privado nuevo `reloadSceneFromJson(...)` — extraído del código ya existente de "Load Scene" (limpia GPU, llama `fromJson`, re-registra GPU incluida la rama skinned), reusado por ambos flujos. `sandbox/src/main.cpp` gatea `physics.stepSimulation`/`scene.update`/`audio.update` tras `renderer.isPlaying()`; en Edit Mode solo llama `scene.getRoot().updateWorldTransforms()` (propagación de transforms sin física) — esto es lo que arregla que hoy no se pueda editar un objeto con collider dinámico, porque `Scene::update` seguía imponiendo la pose de PhysX sobre el `GameObject` en cada frame.

**Tech Stack:** C++20, nlohmann/json (ya integrado, ver `docs/superpowers/plans/2026-07-10-scene-serialization.md`), sin framework de tests unitarios (verificación por compilación + ejecución manual del editor).

## Global Constraints

- No hay gtest/ctest en el repo — cada tarea se verifica con `build.bat` vía **PowerShell** (no Bash). La verificación funcional completa (Play→edición imposible de pelear→Stop→restore) es manual, en la Tarea 3.
- `Scene::toJson`/`fromJson` deben preservar EXACTAMENTE el comportamiento actual de `save`/`load` (mismo JSON, misma atomicidad, mismo manejo de errores) — es una extracción, no un rediseño.
- El estado Play Mode (`m_isPlaying`, `m_playSnapshot`) vive en `EditorUI`, no en `Scene`/`Renderer`/`main.cpp` — mismo sitio que ya orquesta Save/Load Scene, con acceso directo a `m_scene`/`m_physics`/`m_audio`/`m_renderer`.
- El gate del loop en `main.cpp` no debe tocar la clase `Scene` en absoluto — usa `scene.getRoot().updateWorldTransforms()`, ya público vía `GameObject`.
- Editar la escena mientras Play Mode está activo NO se bloquea (decisión de scope ya tomada) — cualquier cambio se descarta igual al pulsar Stop.

---

### Task 1: `Scene::toJson`/`fromJson` — extracción sin cambio de comportamiento

**Files:**
- Modify: `engine/include/DonTopo/Scene.h` (reemplazo completo)
- Modify: `engine/src/Scene.cpp:351-407` (reemplaza `Scene::save`/`Scene::load`)

**Interfaces:**
- Produces: `Scene::toJson() const -> nlohmann::json`, `Scene::fromJson(const nlohmann::json& j, PhysicsManager& physics, AudioManager& audio) -> bool`. `Scene::save`/`Scene::load` mantienen sus firmas actuales, ahora implementadas como wrappers finos.

- [ ] **Step 1: Reemplazar `engine/include/DonTopo/Scene.h`**

```cpp
#pragma once
#include <string>
#include <nlohmann/json_fwd.hpp>
#include "DonTopo/GameObject.h"

namespace DonTopo
{
    class PhysicsManager;
    class AudioManager;

    class Scene
    {
        public:
            explicit Scene(std::string name = "Scene");

            GameObject& getRoot() { return m_root; }
            const GameObject& getRoot() const { return m_root; }

            GameObject* addGameObject(const std::string& name, GameObject* parent = nullptr);
            void removeGameObject(GameObject* node);

            template <typename Fn>
            void traverse(Fn fn) { m_root.traverse(fn); }

            void update(float dt, PhysicsManager& physics);
            void shutdown(PhysicsManager& physics, AudioManager& audio);

            // Serializa el árbol completo (transforms, mesh, colliders, audio
            // clip) a un nlohmann::json en memoria.
            nlohmann::json toJson() const;
            // Reemplaza el árbol actual por el contenido de j. Limpia la
            // escena existente (shutdown + move-assignment) SOLO si j es
            // válido — una carga fallida no modifica la escena en memoria.
            // Recrea colliders/audio vía physics/audio (mismas factories que
            // usa EditorUI). No toca Renderer — el caller debe registrar/
            // liberar los meshes en GPU (ver EditorUI::reloadSceneFromJson).
            bool fromJson(const nlohmann::json& j, PhysicsManager& physics, AudioManager& audio);

            // Serializa el árbol completo a path en formato JSON (vía
            // toJson()). false si la escritura falla.
            bool save(const std::string& path) const;
            // Lee y parsea path, delega en fromJson(...). false si el
            // fichero no existe o el JSON es inválido.
            bool load(const std::string& path, PhysicsManager& physics, AudioManager& audio);

        private:
            std::string m_name;
            GameObject  m_root;
    };
}
```

- [ ] **Step 2: Reemplazar `Scene::save`/`Scene::load` en `engine/src/Scene.cpp:351-407`**

Reemplaza el bloque completo desde `bool Scene::save(const std::string& path) const` hasta el `}` final de `Scene::load` (líneas 351-407) por:

```cpp
    nlohmann::json Scene::toJson() const
    {
        nlohmann::json root;
        root["version"] = 1;
        root["root"] = nodeToJson(m_root);
        return root;
    }

    bool Scene::save(const std::string& path) const
    {
        return FileManager::writeJson(path, toJson());
    }

    bool Scene::fromJson(const nlohmann::json& j, PhysicsManager& physics, AudioManager& audio)
    {
        if (!j.contains("version") || !j["version"].is_number_integer() || j["version"].get<int>() != 1 ||
            !j.contains("root") || !j["root"].is_object())
            return false;

        const nlohmann::json& rootJson = j["root"];

        // Construye el árbol nuevo en un GameObject temporal, desconectado de
        // m_root: si nodeFromJson lanza a mitad de un nodo interno malformado,
        // el temporal se destruye solo al salir de scope (liberando los
        // colliders/audio ya creados en él — physics/audio siguen vivos) y
        // m_root queda intacto. Garantiza que una carga fallida nunca deja la
        // escena a medio reconstruir, no solo en el chequeo de version/root de
        // arriba sino también ante malformación anidada más abajo en el árbol
        // (spec: "carga fallida no modifica la escena").
        GameObject newRoot(rootJson.value("name", "root"));
        try
        {
            nodeFromJson(rootJson, &newRoot, glm::mat4(1.0f), physics, audio);
        }
        catch (const nlohmann::json::exception&)
        {
            return false;
        }

        shutdown(physics, audio);
        m_root = std::move(newRoot);
        // addChild() (llamado dentro de nodeFromJson vía newRoot.addChild/
        // node->addChild) apunta el parent de cada hijo directo al objeto
        // newRoot original — que era una variable local a esta función. Tras
        // el move-assignment, m_root vive en su propia dirección estable (es
        // un miembro de Scene), así que hay que re-apuntar el parent de los
        // hijos directos a &m_root. Los nietos y descendientes más profundos NO
        // necesitan este arreglo: su parent apunta a su padre inmediato, que
        // vive en el heap vía unique_ptr y no se mueve de dirección con este
        // move-assignment.
        m_root.parent = nullptr;
        for (auto& child : m_root.children)
            child->parent = &m_root;

        m_root.updateWorldTransforms();
        return true;
    }

    bool Scene::load(const std::string& path, PhysicsManager& physics, AudioManager& audio)
    {
        auto parsed = FileManager::readJson(path);
        if (!parsed)
            return false;
        return fromJson(*parsed, physics, audio);
    }
```

- [ ] **Step 3: Compilar**

```powershell
& .\build.bat
```
Expected: build sin errores. `nlohmann::json toJson() const` requiere que `Scene.cpp` siga incluyendo `<nlohmann/json.hpp>` completo (ya lo hace, transitivamente vía `FileManager.h`) — `Scene.h` solo necesita el forward-declare.

- [ ] **Step 4: Commit**

```bash
git add engine/include/DonTopo/Scene.h engine/src/Scene.cpp
git commit -m "refactor(scene): extrae Scene::toJson/fromJson de save/load, sin cambio de comportamiento"
```

---

### Task 2: Botón Play/Stop en `EditorUI`

**Files:**
- Modify: `engine/include/DonTopo/EditorUI.h` (nuevo include, método público, método privado, miembros)
- Modify: `engine/src/EditorUI.cpp` (`drawToolbar`, nuevo método `reloadSceneFromJson`, `drawSceneDialog` simplificado)
- Modify: `engine/include/DonTopo/Renderer.h:38` (nuevo forward `isPlaying()`)

**Interfaces:**
- Consumes: `Scene::toJson() const -> nlohmann::json`, `Scene::fromJson(const nlohmann::json&, PhysicsManager&, AudioManager&) -> bool` (Tarea 1); `Renderer::removeGameObject(GameObject*)`, `Renderer::addStaticMesh(const Mesh&) -> int`, `Renderer::addSkinnedMesh(const SkinnedMesh&) -> int` (ya existentes).
- Produces: `EditorUI::isPlaying() const -> bool`, `Renderer::isPlaying() const -> bool` (usado por `main.cpp` en la Tarea 3). `EditorUI::reloadSceneFromJson(const nlohmann::json&) -> bool` (privado, reusado por "Load Scene" y "Stop").

- [ ] **Step 1: Añadir include, método público y miembros nuevos en `engine/include/DonTopo/EditorUI.h`**

En `engine/include/DonTopo/EditorUI.h:8`, tras `#include <glm/glm.hpp>`, añade:

```cpp
#include <nlohmann/json_fwd.hpp>
```

En `engine/include/DonTopo/EditorUI.h:35`, tras `bool isViewportHovered() const { return m_viewportHovered; }`, añade:

```cpp
    // true mientras Play Mode está activo (física + audio corriendo).
    bool isPlaying() const { return m_isPlaying; }
```

En `engine/include/DonTopo/EditorUI.h:90`, tras `void drawSceneDialog();`, añade:

```cpp
    // Limpia GPU de la escena actual, reemplaza su contenido con j (vía
    // Scene::fromJson) y re-registra GPU (estático + skinned) de lo que
    // quede — tanto si fromJson tuvo éxito (árbol nuevo) como si falló
    // (árbol viejo intacto, con índices reseteados antes de liberar GPU).
    // Limpia m_selected si fromJson tuvo éxito. Devuelve lo que devuelva
    // fromJson. Usado por drawSceneDialog (Load Scene) y por el handler de
    // Stop en drawToolbar. false sin efecto si falta algún puntero
    // (m_scene/m_renderer/m_physics/m_audio).
    bool reloadSceneFromJson(const nlohmann::json& j);
```

En `engine/include/DonTopo/EditorUI.h:184`, tras `std::string m_sceneIOError;`, añade:

```cpp

    // Play Mode — snapshot en RAM del árbol justo antes de pulsar Play,
    // restaurado íntegro al pulsar Stop (tipo Unity Play-In-Editor). No se
    // bloquea la edición mientras está activo — cualquier cambio se
    // descarta igual al restaurar.
    bool           m_isPlaying = false;
    nlohmann::json m_playSnapshot;
```

- [ ] **Step 2: Extraer `EditorUI::reloadSceneFromJson` en `engine/src/EditorUI.cpp`**

Localiza `EditorUI::drawSceneDialog()` (línea ~1436). Reemplaza el bloque completo del método (desde `void EditorUI::drawSceneDialog()` hasta su `}` de cierre) por:

```cpp
bool EditorUI::reloadSceneFromJson(const nlohmann::json& j)
{
    if (!m_scene || !m_renderer || !m_physics || !m_audio)
        return false;

    // Libera recursos GPU de la escena actual y resetea sus índices a -1:
    // si fromJson falla más abajo por malformación anidada, m_root sigue
    // siendo este mismo árbol (Scene::fromJson es atómico), y resetear los
    // índices aquí permite que el traverse de re-registro de abajo lo
    // vuelva a registrar igual que si fuera el árbol nuevo — sin esto, el
    // árbol viejo quedaría con índices obsoletos (Renderer::removeGameObject
    // no los resetea) y sin re-registrar tras un fallo, dejando el viewport
    // vacío pese a que los datos de Scene no cambiaron.
    for (auto& child : m_scene->getRoot().children)
    {
        m_renderer->removeGameObject(child.get());
        child->traverse([](GameObject* go) {
            go->staticRenderIndex = -1;
            go->skinnedRenderIndex = -1;
        });
    }

    bool loaded = m_scene->fromJson(j, *m_physics, *m_audio);
    // Se ejecuta tanto si loaded es true (árbol nuevo, índices ya en -1 por
    // construcción) como si es false (árbol viejo intacto, índices
    // reseteados justo arriba) — en ambos casos hay que volver a subir los
    // meshes a GPU.
    m_scene->traverse([this](GameObject* go) {
        if (go->isSkinned())
        {
            if (go->skinnedRenderIndex < 0)
                go->skinnedRenderIndex = m_renderer->addSkinnedMesh(*go->getSkinnedMesh());
        }
        else if (go->hasMesh() && go->staticRenderIndex < 0)
        {
            go->staticRenderIndex = m_renderer->addStaticMesh(*go->getMesh());
        }
    });

    if (loaded)
        m_selected = nullptr; // la selección anterior ya no existe

    return loaded;
}

void EditorUI::drawSceneDialog()
{
    // Mismo motivo que drawMeshDialog/drawAudioClipDialog: se ejecuta cada
    // frame independientemente de m_sceneDlgOpen para drenar el diálogo aunque
    // el usuario lo cierre sin confirmar.
    if (!m_sceneDlgOpen || !m_sceneFileDialog->Display("SceneDlg"))
        return;

    if (m_sceneFileDialog->IsOk())
    {
        std::string path = m_sceneFileDialog->GetFilePathName();

        if (m_sceneDlgIsSave)
        {
            m_sceneIOError = (m_scene && m_scene->save(path)) ? "" : "No se pudo guardar la escena";
        }
        else
        {
            // Valida la estructura básica del JSON ANTES de tocar GPU/Scene:
            // rechaza un fichero top-level corrupto sin tocar nada (fast
            // path, evita el churn de GPU de reloadSceneFromJson). No cubre
            // malformación anidada — para eso, Scene::fromJson es atómico y
            // reloadSceneFromJson cubre ambos desenlaces.
            auto parsed = FileManager::readJson(path);
            bool structureOk = parsed.has_value() &&
                                parsed->contains("version") && (*parsed)["version"].is_number_integer() &&
                                (*parsed)["version"].get<int>() == 1 &&
                                parsed->contains("root") && (*parsed)["root"].is_object();

            m_sceneIOError = (structureOk && reloadSceneFromJson(*parsed)) ? "" : "No se pudo cargar la escena";
        }
    }

    m_sceneFileDialog->Close();
    m_sceneDlgOpen = false;
}
```

- [ ] **Step 3: Añadir el botón Play/Stop al toolbar**

En `engine/src/EditorUI.cpp:193-211` (inicio de `EditorUI::drawToolbar()`, antes del botón Wireframe), reemplaza:

```cpp
void EditorUI::drawToolbar()
{
    ImGuiViewport* vp = ImGui::GetMainViewport();
    ImGui::SetNextWindowPos(vp->Pos);
    ImGui::SetNextWindowSize(ImVec2(vp->Size.x, kToolbarHeight));
    ImGuiWindowFlags flags =
        ImGuiWindowFlags_NoDocking | ImGuiWindowFlags_NoTitleBar |
        ImGuiWindowFlags_NoResize | ImGuiWindowFlags_NoMove |
        ImGuiWindowFlags_NoScrollbar | ImGuiWindowFlags_NoCollapse;
    ImGui::Begin("##Toolbar", nullptr, flags);

    bool wireframe = m_renderer && m_renderer->isWireframeMode();
```

por:

```cpp
void EditorUI::drawToolbar()
{
    ImGuiViewport* vp = ImGui::GetMainViewport();
    ImGui::SetNextWindowPos(vp->Pos);
    ImGui::SetNextWindowSize(ImVec2(vp->Size.x, kToolbarHeight));
    ImGuiWindowFlags flags =
        ImGuiWindowFlags_NoDocking | ImGuiWindowFlags_NoTitleBar |
        ImGuiWindowFlags_NoResize | ImGuiWindowFlags_NoMove |
        ImGuiWindowFlags_NoScrollbar | ImGuiWindowFlags_NoCollapse;
    ImGui::Begin("##Toolbar", nullptr, flags);

    bool canPlay = m_scene && m_physics && m_audio && m_renderer;
    ImGui::BeginDisabled(!canPlay);
    if (m_isPlaying)
    {
        ImGui::PushStyleColor(ImGuiCol_Button, ImGui::GetStyleColorVec4(ImGuiCol_ButtonActive));
        if (ImGui::Button("Stop"))
        {
            m_sceneIOError = reloadSceneFromJson(m_playSnapshot) ? "" : "No se pudo restaurar la escena";
            m_isPlaying = false;
        }
        ImGui::PopStyleColor();
    }
    else
    {
        if (ImGui::Button("Play"))
        {
            m_playSnapshot = m_scene->toJson();
            m_isPlaying = true;
        }
    }
    ImGui::EndDisabled();

    ImGui::SameLine();
    bool wireframe = m_renderer && m_renderer->isWireframeMode();
```

- [ ] **Step 4: Añadir `Renderer::isPlaying()`**

En `engine/include/DonTopo/Renderer.h:38`, tras `bool isViewportHovered() const { return m_editorUI.isViewportHovered(); }`, añade:

```cpp
            bool isPlaying() const { return m_editorUI.isPlaying(); }
```

- [ ] **Step 5: Compilar**

```powershell
& .\build.bat
```
Expected: build sin errores. Nadie llama `renderer.isPlaying()` todavía desde `main.cpp` (eso es la Tarea 3), así que el botón Play/Stop ya funciona visualmente (snapshot/restore vía `reloadSceneFromJson`) pero física/audio siguen corriendo siempre hasta la Tarea 3.

- [ ] **Step 6: Commit**

```bash
git add engine/include/DonTopo/EditorUI.h engine/src/EditorUI.cpp engine/include/DonTopo/Renderer.h
git commit -m "feat(editor): añade botón Play/Stop con snapshot/restore en RAM"
```

---

### Task 3: Gate del loop en `main.cpp` + verificación manual completa

**Files:**
- Modify: `sandbox/src/main.cpp:202-208`

**Interfaces:**
- Consumes: `Renderer::isPlaying() -> bool` (Tarea 2); `GameObject::updateWorldTransforms()` (ya existente, público).

- [ ] **Step 1: Gatear física/audio tras `renderer.isPlaying()`**

En `sandbox/src/main.cpp:202-208`, reemplaza:

```cpp
            if (renderer.isViewportHovered())
                camera.update(window.getNativeWindow(), dt);
            renderer.setCamera(camera);
            audio.update(camera.getPos(), camera.getFront(), camera.getUp());

            physics.stepSimulation(dt);
            scene.update(dt, physics);
```

por:

```cpp
            if (renderer.isViewportHovered())
                camera.update(window.getNativeWindow(), dt);
            renderer.setCamera(camera);

            if (renderer.isPlaying())
            {
                audio.update(camera.getPos(), camera.getFront(), camera.getUp());
                physics.stepSimulation(dt);
                scene.update(dt, physics);
            }
            else
            {
                // Sin física corriendo, pero los transforms padre→hijo se
                // siguen propagando: gizmo/Properties deben seguir
                // funcionando en Edit Mode. Scene::update también hace esto,
                // pero además impone la pose de PhysX sobre cada GameObject
                // con collider dinámico — justo lo que hace imposible editar
                // esos objetos hoy (la física los pelea cada frame). Al
                // saltarnos scene.update() entero en Edit Mode, ese pull ya
                // no ocurre.
                scene.getRoot().updateWorldTransforms();
            }
```

- [ ] **Step 2: Compilar**

```powershell
& .\build.bat
```
Expected: build sin errores.

- [ ] **Step 3: Verificación manual (checklist, sin framework de tests automatizado)**

Ejecutar:
```powershell
& .\build-ninja\sandbox\Sandbox.exe
```

Comprobar en la ventana:
- [ ] La app arranca en Edit Mode (botón dice "Play", física detenida): el cubo dinámico NO cae al arrancar.
- [ ] Seleccionar el cubo dinámico y moverlo con el gizmo/Properties: la posición se queda donde se soltó (ya no pelea con la física — bug motivador de esta feature, resuelto).
- [ ] Pulsar "Play": el botón cambia a "Stop" con el mismo resaltado que usa Wireframe activo. El cubo dinámico empieza a caer y colisiona con el suelo.
- [ ] Añadir un GameObject nuevo (Basic Shapes > Cube) mientras Play Mode está activo.
- [ ] Pulsar "Stop": el botón vuelve a "Play". El cubo dinámico vuelve exactamente a la posición que tenía justo antes de pulsar Play (la editada a mano, no donde cayó). El GameObject añadido durante Play desaparece.
- [ ] Tras Stop, mover el cubo dinámico de nuevo con el gizmo: sigue sin pelear con la física (Edit Mode restaurado correctamente).
- [ ] Guardar la escena (Save Scene) mientras Play Mode está activo, pulsar Stop, y comprobar que el fichero guardado refleja el estado de Play (no el snapshot) — Save Scene sigue actuando sobre el árbol en memoria en el momento de pulsarlo, independientemente de Play/Stop.
- [ ] Cerrar la ventana estando en Play Mode (sin pulsar Stop antes): no crashea.

- [ ] **Step 4: Commit**

```bash
git add sandbox/src/main.cpp
git commit -m "feat(playmode): gatea física/audio del loop principal tras Play Mode"
```

---

## Self-Review

**Cobertura del spec:** §1 (`toJson`/`fromJson`) → Tarea 1. §2 (estado Play Mode en EditorUI/Renderer) → Tarea 2 Steps 1, 4. §3 (restore reusando "Load Scene") → Tarea 2 Steps 2-3. §4 (gate del loop, por qué no basta con saltar solo `stepSimulation`) → Tarea 3 Step 1. §5 (manejo de errores) → Tarea 2 Step 3 (mensaje "No se pudo restaurar la escena" en Stop). §6 (testing) → Tarea 3 Step 3. Riesgo del spec sobre `<nlohmann/json_fwd.hpp>`: aplicado en Tarea 1 Step 1. Riesgo sobre `ImGui::BeginDisabled` si falta algún puntero: aplicado en Tarea 2 Step 3 (`canPlay`).

**Placeholders:** ninguno — cada paso trae código completo, before/after exacto contra el estado actual real de los ficheros (verificado leyendo el código en el momento de escribir este plan).

**Consistencia de tipos:** `Scene::toJson() const -> nlohmann::json` y `Scene::fromJson(const nlohmann::json&, PhysicsManager&, AudioManager&) -> bool` usan las mismas firmas en la declaración (Tarea 1 Step 1) y su uso en `EditorUI::reloadSceneFromJson`/`drawToolbar` (Tarea 2). `EditorUI::reloadSceneFromJson(const nlohmann::json&) -> bool` firma consistente entre declaración (Tarea 2 Step 1) e implementación/uso (Tarea 2 Steps 2-3). `Renderer::isPlaying() -> bool` consistente entre Tarea 2 Step 4 y su uso en Tarea 3 Step 1.
